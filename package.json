{
  "name": "haoming-dynamic-engine",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-single-html.mjs",
    "check": "npm run build && npm test"
  }
}
